apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "technician.fullname" . }}-config
  labels:
    {{- include "technician.labels" . | nindent 4 }}
data:
  {{- range $name, $content := .Values.config }}
  {{ $name }}: |
    {{- $content | nindent 4 }}
  {{- end }}
